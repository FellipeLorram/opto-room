import { motion } from "framer-motion";
import styled from "styled-components";

export const PlanSelectionContainer = styled(motion.div)`
  width: 100%;
  z-index: 2;
  display: flex;
  align-items: flex-start;
  justify-content: center;
  flex-direction: row;
  align-self: stretch;
  gap: 3rem;
`